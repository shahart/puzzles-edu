import edu.generalpuzzle.examples.cube.dimension3.Grid3D;

import edu.generalpuzzle.infra.IGrid;

IGrid grid;

// user code //////
///////////////////

grid = new Grid3D();
grid.build3d( 4,4,4);

/*
for (int plane=0; plane<4; ++plane)
    for (int y=0; y<plane; ++y)
        for (int x=0; x<4; ++x) {
            grid.remove(new CellId(grid.calcId(y,x,plane)));
        }
*/
                
// end of user code
///////////////////

return grid;
